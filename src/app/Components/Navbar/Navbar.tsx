import React from 'react';

interface Navbar {
    name: string;
    id: number;
}

const Navbar = (props: Navbar) => {
    const {} = props;
    return (
        <div className="">Navbar</div>
    )
};

export default Navbar;